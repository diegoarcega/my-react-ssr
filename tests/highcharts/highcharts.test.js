const assert = require('chai').assert

describe('Gauge Component', function(){

  it('should get data from the widget attribute', function(done){
    assert(1,1)
    done()
  })

  it('should receive a WIDGET object in this.props', function(done){
    assert(1,1)
    done()
  })

  it('should receive a WIDGETS object in this.props', function(done){
    assert(1,1)
    done()
  })

  it('the widget object should have a type that matches the display configuration', function(done){
    assert(1,1)
    done()
  })

  it('the widgets object should contain the data for the widget to be displayed', function(done){
    assert(1,1)
    done()
  })
})
